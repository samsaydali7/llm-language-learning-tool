package com.languagelearning.structure.entity;

/** Level of organization within a book's structure tree. */
public enum StructureNodeType {
    BOOK,
    VOLUME,
    CHAPTER,
    SECTION,
    SUBSECTION
}
