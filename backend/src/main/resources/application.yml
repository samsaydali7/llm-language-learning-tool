server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

ollama:
  base-url: ${OLLAMA_BASE_URL:http://host.docker.internal:11434}

llm:
  exercise-model: ${EXERCISE_MODEL:llama3.2:3b}
